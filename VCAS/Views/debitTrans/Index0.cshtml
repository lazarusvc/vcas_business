@model IEnumerable<VCAS.Models.VCAS_debitTrans>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VCAS_debitAccounts.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VCAS_debitAccounts.amount)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VCAS_debitAccounts.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VCAS_debitAccounts.amount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
